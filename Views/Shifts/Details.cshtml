@model WorkFlowManager.Models.WorkShift
@inject IHtmlLocalizer<SharedResource> L

@{
    ViewData["Title"] = L["ShiftDetails"].Value;
}

<div class="page-header">
    <h1><i class="bi bi-calendar-event-fill"></i>@L["ShiftDetails"]</h1>
</div>

<div class="row">
    <div class="col-md-6">
        <div class="card-custom">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-calendar-check-fill me-2"></i>@Model.StartTime.ToString("dddd, dd MMMM yyyy")</h5>
            </div>
            <div class="card-body">
                <div class="mb-3">
                    <label class="form-label fw-semibold" style="color: #64748b;">
                        <i class="bi bi-person-fill me-1"></i>@L["Employee"]
                    </label>
                    <p class="mb-0">
                        @if (Model.User != null)
                        {
                            <strong style="color: #1e293b;">@Model.User.FirstName @Model.User.LastName</strong>
                            <span class="d-block small" style="color: #64748b;">@Model.User.Email</span>
                        }
                        else
                        {
                            <span style="color: #94a3b8;">@L["Unknown"]</span>
                        }
                    </p>
                </div>
                
                <div class="row mb-3">
                    <div class="col-6">
                        <label class="form-label fw-semibold" style="color: #64748b;">
                            <i class="bi bi-clock me-1"></i>@L["StartTime"]
                        </label>
                        <p class="mb-0">
                            <span class="badge-custom-success" style="font-size: 1.1rem;">@Model.StartTime.ToString("HH:mm")</span>
                        </p>
                    </div>
                    <div class="col-6">
                        <label class="form-label fw-semibold" style="color: #64748b;">
                            <i class="bi bi-clock-fill me-1"></i>@L["EndTime"]
                        </label>
                        <p class="mb-0">
                            <span class="badge-custom-danger" style="font-size: 1.1rem;">@Model.EndTime.ToString("HH:mm")</span>
                        </p>
                    </div>
                </div>
                
                <div>
                    <label class="form-label fw-semibold" style="color: #64748b;">
                        <i class="bi bi-hourglass-split me-1"></i>@L["Duration"]
                    </label>
                    <p class="mb-0">
                        <span class="badge-custom-info" style="font-size: 1.1rem;">@Model.DurationHours.ToString("F1") @L["Hours"]</span>
                    </p>
                </div>
            </div>
            <div class="card-footer d-flex gap-2">
                <a asp-action="Index" class="btn btn-custom-secondary">
                    <i class="bi bi-arrow-left me-1"></i>@L["BackToList"]
                </a>
                <form asp-action="Delete" asp-route-id="@Model.Id" method="post" class="d-inline"
                      onsubmit="return confirm('@L["ConfirmDeleteShift"]');">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-custom-danger">
                        <i class="bi bi-trash-fill me-1"></i>@L["Delete"]
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
